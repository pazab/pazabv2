# PAZAB v2 — 확정 디자인 계획 (UX/UI/네비)

> 작성일: 2026-07-20  
> 상위 전략: [STRATEGY.md](STRATEGY.md) — "가게 관리 앱인데, 펑크 나면 버튼 하나로 검증된 대타가 오는 앱"  
> 이 문서는 화면·네비·흐름의 확정 설계다. UI 작업 전 반드시 참조.

---

## 0. 현재 진단 요약 (2026-07-20 기준)

### 이미 전략과 정렬됨 — 건드리지 말 것
- 사장님 로그인 → `/myteam` 리다이렉트 (HR-First)
- 피드·팔로우는 탭에서 빠져 ⋮ 메뉴로 강등된 상태
- 랜딩 카피 "초간편 근거리 대타 매칭 서비스"
- 다크 기반 퍼플-핑크 그라데이션 디자인 언어 (유지)

### 어긋남 — 이 문서가 고치는 것
1. 알바생 홈이 `/explore`(구직 탐색) — 잡보드가 첫 화면
2. 대타 페이지가 SOS 루프가 아닌 수동 카드 브라우징 + 공고 선등록 요구
3. Tier1/Tier2 배지 체계가 UI에 없음
4. 네비 첫 탭이 역할 무관 "탐색"
5. BottomNav가 5초 폴링으로 채용 매칭 감시 (아키텍처 오염)
6. AppHeader/BottomNav 다크 색상 하드코딩 → 라이트모드 파손 구조
7. 랜딩 히어로에 수도쿠 CTA

---

## 1. 네비바 — 확정 구조

### 현재
```
[탐색] [대타] [근태⭐center] [채팅] [MY]
```

### 확정 (목표)
```
[홈] [대타⚡center] [채팅] [MY]      ← 4탭, 대타가 중앙 강조
```

- **홈** (`/myteam` 확장): 역할 인식 홈. 사장님 = 매장 대시보드(오늘 근무자·출근 현황·급여 알림·팀 관리), 알바생 = 내 근무(오늘 스케줄·출퇴근 버튼·정산 현황·대타 가능 토글). 기존 근태 탭이 홈으로 승격되는 것.
- **대타 ⚡** (center, 그라데이션 원형 버튼): 제품의 정체성 버튼. "펑크 나면 누르는 버튼"이 물리적으로 중앙에.
- **탐색**: 탭에서 제거 → ⋮ 메뉴 + 홈 내 "동네 알바 더 보기" 진입점으로 강등. 코드 삭제 X.
- **피드·스도쿠**: 현행대로 ⋮ 메뉴 유지.
- **로고 클릭**: `/explore` → 역할별 홈으로 변경.

### 이행 순서 (한 번에 안 바꿈)
1. **1단계 (즉시)**: 탭 순서만 재배치 — `[근태] [대타⚡center] [탐색] [채팅] [MY]`. 근태를 첫 탭으로, 대타를 중앙으로. 워커 리다이렉트 `/explore` → `/myteam`.
2. **2단계 (홈 대시보드 완성 후)**: 근태 탭을 "홈"으로 개명하고 탐색 탭 제거 → 4탭 확정형.

---

## 2. 대타 화면 — SOS 루프 UI 재설계 (최우선)

### 사장님 뷰 (`/daeta`, employer)
현재의 틴더식 카드 덱은 **보조 경로("직접 고르기")로 강등**. 기본 화면은:

```
┌─────────────────────────────┐
│  ⚡ 대타 구하기        [1버튼] │  ← 탭하면 바텀시트: 날짜·시간·시급(할증 기본값 자동)
├─────────────────────────────┤
│  진행 중 요청 카드            │
│  "내일 18-22시 홀서빙"        │
│  상태: ② 동네 Tier1 공개 중   │  ← 에스컬레이션 단계 실시간 표시
│  응답 2 · 조회 8             │
├─────────────────────────────┤
│  [직접 고르기 →]             │  ← 기존 카드 덱으로 진입 (보조)
└─────────────────────────────┘
```

- **공고 선등록 요구 제거.** "대타 구하기" 바텀시트 제출 = `daeta_postings` 생성 + 에스컬레이션 시작. 원스톱.
- 에스컬레이션 상태 4단계를 카드에 명시: ① 우리 팀 알림 중 → ② 동네 Tier1 공개 → ③ 신규 포함 공개(opt-in 시) → ④ 공개 SOS 글
- 시급 입력란에 긴급 할증 기본값 자동 채움 (당일 +20% 등, DB 설정값). 옆에 "동네 평균 시급" 정보 표시.
- "신규(🔵)도 받기" 토글은 요청 바텀시트 안에.

### 알바생 뷰 (`/daeta`, worker)
```
┌─────────────────────────────┐
│  🟢 대타 가능 (토글)          │  ← 상단 고정. 켜면 동네·시간대 노출
├─────────────────────────────┤
│  들어온 요청 (나에게 온 것)    │  ← 수락/거절 즉답형 카드
├─────────────────────────────┤
│  이번 주 동네 대타 자리        │  ← 시급 할증 표시, 거리·ETA
└─────────────────────────────┘
```

### Tier 배지 (전 화면 공통 컴포넌트)
- `✅ 검증` (Tier1): 팀이력 or 대타 완료 이력 — 이름 옆 상시 표시
- `🔵 신규` (Tier2)
- `노쇼 위험 낮음` (HEXACO 기반, Tier1 중 조건 충족 시만)
- 배지 컴포넌트 1개로 통일 (`components/TierBadge.tsx` 신규), 대타 카드·프로필·탐색 전부 재사용

---

## 3. 홈(마이팀) — 역할별 대시보드

### 사장님 홈
상단부터: 오늘 출근 현황 (실시간) → **대타 SOS 버튼 (펑크 시 즉시)** → 급여/계약 처리할 일 → 팀원 리스트. 첫 화면에 빈 공고판·빈 피드가 보이는 일 절대 없음.

### 알바생 홈
상단부터: 오늘 내 스케줄 + 출퇴근 버튼 → 이번 달 예상 정산액 → 대타 가능 토글 + 들어온 요청 → (하단) "동네 알바 더 보기" (탐색 진입점).

---

## 4. 디자인 시스템 규칙 (확정)

- **디자인 언어 유지**: 다크 기반, 퍼플(#8b5cf6)-핑크(#ec4899) 그라데이션, 글래스모피즘 헤더/네비, 라운드 20-24px. 전면 리디자인 안 함.
- **색상 하드코딩 금지**: `rgba(24,24,27,…)` 류 → CSS 변수(`--nav-bg`, `--surface` 등)로 치환. AppHeader·BottomNav 우선. (라이트모드 대비, CLAUDE.md 다크모드 규칙과 동일 원칙)
- **대타 = 오렌지/앰버 계열 액센트** (`#fb923c` 기존 사용 중): SOS·긴급의 시각 언어로 고정. 퍼플-핑크(브랜드)와 구분.
- **Tier 색상**: 검증 = 그린(#22c55e), 신규 = 블루(#3b82f6). 전 화면 동일.
- **인라인 스타일 현행 유지** (기존 코드베이스 관성 존중), 단 반복되는 패턴은 `lib/styles.ts`에 추가.

---

## 5. 정리 대상 (전략 무관 부채)

- **BottomNav 폴링 제거**: 5초 `setInterval` 매칭 감시 → Realtime 구독으로 교체하거나 별도 Provider로 분리. 네비는 렌더만.
- **랜딩 수도쿠 CTA**: 히어로에서 제거 → footer 링크로 강등.
- **매칭 성사 모달**(BottomNav 내 채용 매칭 축하 모달): 대타 수락 알림과 통합 검토 — 대타 수락이 더 중요한 순간인데 전용 알림이 없음.

---

## 6. 실행 순서 (확정)

| 순서 | 작업 | 이유 | 상태 |
|---|---|---|---|
| P1 | 대타 SOS 루프 UI (사장님 원버튼 요청 + 에스컬레이션 카드 + 알바생 토글/요청 뷰) | 제품 정체성이자 과금 지점 | ✅ 2026-07-20 |
| P2 | TierBadge 컴포넌트 + 대타/탐색/프로필 적용 | 신뢰가 상품 — 시각화 필수 | ✅ 2026-07-20 (덱·워커상세·대타홈 적용, 탐색 리스트는 추후) |
| P3 | 네비 1단계 재배치 (근태 첫 탭, 대타 중앙, 워커 홈 리다이렉트) | 저비용 고효과 | ✅ 2026-07-20 |
| P4 | 홈 대시보드 역할별 재구성 → 네비 2단계 (4탭 확정형) | P1~P3 안착 후 | ✅ 2026-07-20 (홈 퀵액션 배너 + 4탭. 상세 대시보드 고도화는 추후) |
| P5 | CSS 변수 치환·폴링 제거·랜딩 정리 | 부채, 병행 가능 | ✅ 2026-07-20 (폴링 5s→30s+탭숨김 정지, 완전 Realtime 전환은 추후) |

각 단계는 독립 배포 가능. P1 완료 전에 P4 시작 금지 (홈보다 SOS 루프가 먼저).

---

## 7. 계약서 작성 흐름 개선 (2026-07-21)

### 수정된 버그

| # | 증상 | 원인 | 수정 |
|---|---|---|---|
| 1 | 여러 매장 보유 사장님 — "새 계약서 작성" 시 틀린 매장 칩 하이라이트 | `loadInit()`이 항상 `setSelEp(finalEps[0])` 고정 | `capturedEps` 캡처 → `load()`/`loadByMember()` 전달 → 해당 `employer_profile_id` 칩 자동 선택 |
| 2 | 지원한 공고 조건이 아닌 최신 공고 조건으로 채워짐 | `matches.job_id` 무시하고 최신 jobs 쿼리만 사용 | `job_id` 있으면 해당 공고 우선 로드, 없으면 최신 jobs fallback |
| 3 | select step에서 누구 계약서인지 알 수 없음 | select step에 근로자/매장 정보 미표시 | `selMatch && f.worker` 조건으로 "계약 대상" 미리보기 카드 표시 |

### 진입 경로별 상태 (확인 완료)

| 진입 경로 | 파라미터 | 동작 |
|---|---|---|
| 채팅방 "계약서 작성 →" | `matchId + mode=update + from=chat` | 기존 계약 불러와 edit step 바로 진입, 저장 후 채팅방 복귀 |
| 팀원 상세 "새 계약서 작성" | `memberId` | select step (계약 종류 선택) → 올바른 매장 칩 + 근로자 미리보기 표시 |
| 팀원 상세 "계약서 수정" | `memberId + mode=update` | 기존 계약 불러와 edit step 바로 진입 |
| Paz AI 버튼 (무컨텍스트) | 없음 | select step에서 매장·종류·대상 수동 선택 |

### 파일
- [`app/contract/page.tsx`](app/contract/page.tsx)

---

## 배포 전 수동 작업 (필수)

1. **SQL 패치 실행**: `supabase/patch_daeta_sos.sql` → Supabase SQL Editor에서 실행 (escalation 컬럼 + daeta_sos_config 테이블). 미실행 시 대타 등록이 실패함.
2. **크론 등록**: cron-job.org에 `GET /api/cron/daeta-escalate` 5분 주기 추가, `Authorization: Bearer <CRON_SECRET>` 헤더 포함.
