# PAZAB v2 — 확정 디자인 계획 (UX/UI/네비)

> 작성일: 2026-07-20  
> 상위 전략: [STRATEGY.md](STRATEGY.md) — "가게 관리 앱인데, 펑크 나면 버튼 하나로 검증된 대타가 오는 앱"  
> 이 문서는 화면·네비·흐름의 확정 설계다. UI 작업 전 반드시 참조.

---

## 0. 현재 진단 요약 (2026-07-20 기준)

### 이미 전략과 정렬됨 — 건드리지 말 것
- 사장님 로그인 → `/myteam` 리다이렉트 (HR-First)
- 피드·팔로우는 탭에서 빠져 ⋮ 메뉴로 강등된 상태
- 랜딩 카피 "초간편 근거리 대타 매칭 서비스"
- 다크 기반 퍼플-핑크 그라데이션 디자인 언어 (유지)

### 어긋남 — 이 문서가 고치는 것
1. 알바생 홈이 `/explore`(구직 탐색) — 잡보드가 첫 화면
2. 대타 페이지가 SOS 루프가 아닌 수동 카드 브라우징 + 공고 선등록 요구
3. Tier1/Tier2 배지 체계가 UI에 없음
4. 네비 첫 탭이 역할 무관 "탐색"
5. BottomNav가 5초 폴링으로 채용 매칭 감시 (아키텍처 오염)
6. AppHeader/BottomNav 다크 색상 하드코딩 → 라이트모드 파손 구조
7. 랜딩 히어로에 수도쿠 CTA

---

## 1. 네비바 — 확정 구조

### 현재
```
[탐색] [대타] [근태⭐center] [채팅] [MY]
```

### 확정 (목표)
```
[홈] [대타⚡center] [채팅] [MY]      ← 4탭, 대타가 중앙 강조
```

- **홈** (`/myteam` 확장): 역할 인식 홈. 사장님 = 매장 대시보드(오늘 근무자·출근 현황·급여 알림·팀 관리), 알바생 = 내 근무(오늘 스케줄·출퇴근 버튼·정산 현황·대타 가능 토글). 기존 근태 탭이 홈으로 승격되는 것.
- **대타 ⚡** (center): 제품의 정체성 버튼. "펑크 나면 누르는 버튼"이 물리적으로 중앙에. ~~그라데이션 원형 FAB~~ → 2026-07-22: 배지가 생기면서 다른 탭과 같은 "확인하러 가는 목적지"가 돼 크기는 통일, 오렌지 색만 유지(§8.2).
- **탐색**: 탭에서 제거 → ⋮ 메뉴 + 홈 내 "동네 알바 더 보기" 진입점으로 강등. 코드 삭제 X.
- **피드·스도쿠**: 현행대로 ⋮ 메뉴 유지.
- **로고 클릭**: `/explore` → 역할별 홈으로 변경.

### 이행 순서 (한 번에 안 바꿈)
1. **1단계 (즉시)**: 탭 순서만 재배치 — `[근태] [대타⚡center] [탐색] [채팅] [MY]`. 근태를 첫 탭으로, 대타를 중앙으로. 워커 리다이렉트 `/explore` → `/myteam`.
2. **2단계 (홈 대시보드 완성 후)**: 근태 탭을 "홈"으로 개명하고 탐색 탭 제거 → 4탭 확정형.

---

## 2. 대타 화면 — SOS 루프 UI 재설계 (최우선)

### 사장님 뷰 (`/daeta`, employer)
현재의 틴더식 카드 덱은 **보조 경로("직접 고르기")로 강등**. 기본 화면은:

```
┌─────────────────────────────┐
│  ⚡ 대타 구하기        [1버튼] │  ← 탭하면 바텀시트: 날짜·시간·시급(할증 기본값 자동)
├─────────────────────────────┤
│  진행 중 요청 카드            │
│  "내일 18-22시 홀서빙"        │
│  상태: ② 동네 Tier1 공개 중   │  ← 에스컬레이션 단계 실시간 표시
│  응답 2 · 조회 8             │
├─────────────────────────────┤
│  [직접 고르기 →]             │  ← 기존 카드 덱으로 진입 (보조)
└─────────────────────────────┘
```

- **공고 선등록 요구 제거.** "대타 구하기" 바텀시트 제출 = `daeta_postings` 생성 + 에스컬레이션 시작. 원스톱.
- 에스컬레이션 상태 4단계를 카드에 명시: ① 우리 팀 알림 중 → ② 동네 Tier1 공개 → ③ 신규 포함 공개(opt-in 시) → ④ 공개 SOS 글
- 시급 입력란에 긴급 할증 기본값 자동 채움 (당일 +20% 등, DB 설정값). 옆에 "동네 평균 시급" 정보 표시.
- "신규(🔵)도 받기" 토글은 요청 바텀시트 안에.

### 알바생 뷰 (`/daeta`, worker)
```
┌─────────────────────────────┐
│  🟢 대타 가능 (토글)          │  ← 상단 고정. 켜면 동네·시간대 노출
├─────────────────────────────┤
│  들어온 요청 (나에게 온 것)    │  ← 수락/거절 즉답형 카드
├─────────────────────────────┤
│  이번 주 동네 대타 자리        │  ← 시급 할증 표시, 거리·ETA
└─────────────────────────────┘
```

### Tier 배지 (전 화면 공통 컴포넌트)
- `✅ 검증` (Tier1): 팀이력 or 대타 완료 이력 — 이름 옆 상시 표시
- `🔵 신규` (Tier2)
- `노쇼 위험 낮음` (HEXACO 기반, Tier1 중 조건 충족 시만)
- 배지 컴포넌트 1개로 통일 (`components/TierBadge.tsx` 신규), 대타 카드·프로필·탐색 전부 재사용

---

## 3. 홈(마이팀) — 역할별 대시보드

### 사장님 홈
상단부터: 오늘 출근 현황 (실시간) → **대타 SOS 버튼 (펑크 시 즉시)** → 급여/계약 처리할 일 → 팀원 리스트. 첫 화면에 빈 공고판·빈 피드가 보이는 일 절대 없음.

### 알바생 홈
상단부터: 오늘 내 스케줄 + 출퇴근 버튼 → 이번 달 예상 정산액 → 대타 가능 토글 + 들어온 요청 → (하단) "동네 알바 더 보기" (탐색 진입점).

---

## 4. 디자인 시스템 규칙

> ⚠️ **2026-07-22 방향 전환**: "그라데이션·글래스모피즘 유지, 전면 리디자인 안 함" 조항을 **폐기**. 토스 지향 "방향 A: 플랫 미니멀"로 교체. 이유: 화면마다 다른 그라데이션·글로우가 "AI 데모/랜딩페이지" 톤을 만들어 실제 앱처럼 안 느껴진다는 판단(§9 참조).

- **방향 A: 플랫 미니멀** — 그라데이션은 `--gradient-hero` 토큰 하나만, 대타 SOS 등 화면당 최대 1곳 예외. 나머지는 전부 단색.
- **색상은 전부 CSS 변수로만 참조** — 컴포넌트에 hex 하드코딩 금지. `app/globals.css`의 토큰(`--primary`, `--accent`, `--success`, `--warning`, `--danger`, `--surface`, `--border` 등)과 `lib/styles.ts`의 공용 스타일(`btnPrimary`, `btnHero`, `chipWarning` 등)만 사용. 색을 바꿀 땐 컴포넌트가 아니라 이 두 파일만 고치면 전체가 바뀌는 구조.
- **글로우 섀도우 금지** — `boxShadow`에 색상 있는 글로우(`rgba(124,58,237,0.35)` 류) 대신 `var(--shadow-elevate)`(무채색, 라이트/다크 각각 정의됨)만 사용.
- **대타 = 오렌지/앰버 계열 액센트** (`--gradient-hero`, 텍스트는 `#fb923c`): SOS·긴급의 시각 언어로 고정. 브랜드 컬러(퍼플)와 구분되는 유일한 예외.
- **Tier 색상**: 검증 = 그린(`var(--success)`), 신규 = 블루(#3b82f6). 전 화면 동일.
- **아이콘 시스템 통일 (진행 중)**: 이모지를 기능 아이콘으로 쓰던 곳(💼🏪📅 등)은 점진적으로 `ti ti-*` 아이콘폰트로 교체. 이모지는 감성적 순간(매칭 성사 🎉, 경고 ⚠️)에만 허용.
- **인라인 스타일 현행 유지** (기존 코드베이스 관성 존중), 단 색상 값은 반드시 위 토큰을 참조.

---

## 5. 정리 대상 (전략 무관 부채)

- **BottomNav 폴링 제거**: 5초 `setInterval` 매칭 감시 → Realtime 구독으로 교체하거나 별도 Provider로 분리. 네비는 렌더만.
- **랜딩 수도쿠 CTA**: 히어로에서 제거 → footer 링크로 강등. ✅ 2026-07-22: footer 링크 + `AppHeader` `⋮` 메뉴 항목까지 완전 제거(진입 경로만 차단, `app/sudoku/*` 코드는 보존 — 재연결 시 두 곳에 버튼만 되돌리면 됨).
- **매칭 성사 모달**(BottomNav 내 채용 매칭 축하 모달): 대타 수락 알림과 통합 검토 — 대타 수락이 더 중요한 순간인데 전용 알림이 없음.

---

## 6. 실행 순서 (확정)

| 순서 | 작업 | 이유 | 상태 |
|---|---|---|---|
| P1 | 대타 SOS 루프 UI (사장님 원버튼 요청 + 에스컬레이션 카드 + 알바생 토글/요청 뷰) | 제품 정체성이자 과금 지점 | ✅ 2026-07-20 |
| P2 | TierBadge 컴포넌트 + 대타/탐색/프로필 적용 | 신뢰가 상품 — 시각화 필수 | ✅ 2026-07-20 (덱·워커상세·대타홈 적용, 탐색 리스트는 추후) |
| P3 | 네비 1단계 재배치 (근태 첫 탭, 대타 중앙, 워커 홈 리다이렉트) | 저비용 고효과 | ✅ 2026-07-20 |
| P4 | 홈 대시보드 역할별 재구성 → 네비 2단계 (4탭 확정형) | P1~P3 안착 후 | ✅ 2026-07-20 (홈 퀵액션 배너 + 4탭. 상세 대시보드 고도화는 추후) |
| P5 | CSS 변수 치환·폴링 제거·랜딩 정리 | 부채, 병행 가능 | ✅ 2026-07-20 (폴링 5s→30s+탭숨김 정지, 완전 Realtime 전환은 추후) |
| P6 | UX 리뷰 반영 (헤더·네비·SOS 확산 경고·홈 요약·사전미팅 기본값·수도쿠 강등) | 전세대 접근성 재검토 결과 | ✅ 2026-07-22 (아래 §8 참조) |

각 단계는 독립 배포 가능. P1 완료 전에 P4 시작 금지 (홈보다 SOS 루프가 먼저).

---

## 7. 계약서 작성 흐름 개선 (2026-07-21)

### 수정된 버그

| # | 증상 | 원인 | 수정 |
|---|---|---|---|
| 1 | 여러 매장 보유 사장님 — "새 계약서 작성" 시 틀린 매장 칩 하이라이트 | `loadInit()`이 항상 `setSelEp(finalEps[0])` 고정 | `capturedEps` 캡처 → `load()`/`loadByMember()` 전달 → 해당 `employer_profile_id` 칩 자동 선택 |
| 2 | 지원한 공고 조건이 아닌 최신 공고 조건으로 채워짐 | `matches.job_id` 무시하고 최신 jobs 쿼리만 사용 | `job_id` 있으면 해당 공고 우선 로드, 없으면 최신 jobs fallback |
| 3 | select step에서 누구 계약서인지 알 수 없음 | select step에 근로자/매장 정보 미표시 | `selMatch && f.worker` 조건으로 "계약 대상" 미리보기 카드 표시 |

### 진입 경로별 상태 (확인 완료)

| 진입 경로 | 파라미터 | 동작 |
|---|---|---|
| 채팅방 "계약서 작성 →" | `matchId + mode=update + from=chat` | 기존 계약 불러와 edit step 바로 진입, 저장 후 채팅방 복귀 |
| 팀원 상세 "새 계약서 작성" | `memberId` | select step (계약 종류 선택) → 올바른 매장 칩 + 근로자 미리보기 표시 |
| 팀원 상세 "계약서 수정" | `memberId + mode=update` | 기존 계약 불러와 edit step 바로 진입 |
| Paz AI 버튼 (무컨텍스트) | 없음 | select step에서 매장·종류·대상 수동 선택 |

### 파일
- [`app/contract/page.tsx`](app/contract/page.tsx)

---

## 8. UX 리뷰 반영 (2026-07-22)

전세대 접근성 4원칙(인지부하·Recognition>Recall·오류방지·접근성) 기준 재검토 결과 반영.

### 8.1 AppHeader `⋮` 메뉴 재구성
- 대타 SOS 항목 제거(하단 네비 중앙과 중복 노출이라 오히려 혼란) — 이미 네비에 있는 기능을 메뉴에도 넣지 않음
- 탐색·피드를 "둘러보기" 섹션 라벨 + 톤다운(회색)으로 분리, 설정과 시각적으로 구분
- 스도쿠 항목 제거 (§5 참조, 코드는 보존)
- 파일: [`components/AppHeader.tsx`](components/AppHeader.tsx)

### 8.2 BottomNav — 대타 탭 배지 + 크기 통일
- 대타 탭에 `notifications` 테이블(`type=daeta`, `is_read=false`) 실시간 배지 추가 — 평소에도 "볼 게 있다/없다"를 눌러보지 않고 인식 가능
- 중앙 대타 버튼의 오버사이즈 원형 FAB를 제거하고 다른 3탭과 동일 크기로 통일. 배지가 생기면서 대타 탭도 "확인하러 가는 목적지"(채팅·MY와 동일 역할)가 됐는데 크기만 튀는 게 실제 역할과 안 맞았음. 색(오렌지)만 유지해서 우선순위 힌트는 남김
- 파일: [`components/BottomNav.tsx`](components/BottomNav.tsx)

### 8.3 대타 SOS 확산 임박 경고
- 에스컬레이션 단계 전환 5분 전, 사장님에게 "⏳ N분 뒤 OO으로 확대돼요, 취소하려면 지금" 알림 자동 발송(스키마 변경 없이 `notifications.data`로 중복 발송 방지)
- `DaetaSosHome`의 진행 중 요청 카드에도 동일 카운트다운을 노란 배너로 표시 — 푸시를 놓쳐도 화면에서 인지·취소 가능
- 파일: [`lib/daetaEscalation.ts`](lib/daetaEscalation.ts), [`components/daeta/DaetaSosHome.tsx`](components/daeta/DaetaSosHome.tsx)

### 8.4 매칭 성사 모달 — 기본 액션 변경
- "AI 사전미팅"이 주 버튼이던 걸 "바로 채팅하기"를 주 버튼으로, AI 사전미팅은 보조("선택") 버튼으로 순서·강조 변경. 급한 대타 매칭 순간에 속도가 최우선이라는 원칙과 맞춤
- 파일: [`components/BottomNav.tsx`](components/BottomNav.tsx)

### 8.5 홈(`/myteam`) — 접힘/펼침 통계 통일
- 시행착오: both 계정(사장님+알바생 겸업)에 `/daeta`식 역할 토글(탭)을 먼저 시도했으나 폐기 — "상태 조회"는 "다른 액션 모드"가 아니라서, 탭으로 가리면 안 보는 쪽에서 벌어지는 일(계약서 미확인 등)을 놓칠 위험이 있음(Error Prevention 위반)
- 최종: 데이터 있는 섹션은 접어서 요약만, 데이터 없는 섹션은 펼쳐서 온보딩 CTA 노출. 접힌 요약은 펼쳤을 때 나오는 것과 **동일한 통계 그리드 컴포넌트**(라벨 위/숫자 아래, 얇은 구분선)를 재사용 — 접힘↔펼침 전환 시 디자인이 바뀌는 느낌 없음
- 파일: [`app/myteam/page.tsx`](app/myteam/page.tsx)

### 8.6 기능 스코프 재검토 — 핵심 vs 부가
- HEXACO 인터뷰(`/interview`)는 이미 온보딩 필수 단계가 아님(선택적 CTA로만 노출) — 확인만 하고 변경 없음
- 스도쿠는 한 줄 정의("펑크나면 버튼 하나로 대타 오는 앱")와 무관 → §5·8.1 참조, 진입 경로 제거
- AI 사전미팅은 §8.4로 선택사항화

---

## 9. 디자인 방향 전환 — "방향 A: 플랫 미니멀" (2026-07-22)

### 진단
실제로 모바일에서 써보니 "앱 같지 않다"는 피드백 → 원인은 터치/스크롤 동작이 아니라 **비주얼 자체가 데모/랜딩페이지 톤**이었음:
- 버튼·아이콘·카드마다 다른 그라데이션(퍼플-핑크, 오렌지-레드, 그린-에메랄드 등)이 화면 하나에 다 섞여 있어 "브랜드 컬러 하나"가 없었음
- 색 있는 글로우 섀도우(`boxShadow: rgba(124,58,237,0.35)` 류)가 버튼마다 있어 발표용 데모 느낌
- 이모지와 아이콘폰트(`ti ti-*`)가 기능 아이콘 자리에서 혼용
- 매장 카드마다 랜덤하게 다른 색 그라데이션을 돌리는 등, 정보와 무관한 장식적 색 변주가 많았음

### 방향
토스·카카오식 "그라데이션은 화면당 1곳(히어로)만, 나머지는 단색" 원칙으로 전환. §4 규칙 갱신.

### 아키텍처 — 색을 코드(로직)와 분리
- `app/globals.css`에 방향 A 토큰 추가: `--warning`/`--warning-bg`/`--warning-border`, `--danger-*`, `--success-*`, `--gradient-hero`(유일한 그라데이션 예외), `--shadow-elevate`(무채색 그림자), 라이트/다크 각각 정의
- `lib/styles.ts`에 `btnPrimary`/`btnAccent`를 그라데이션→단색으로 교체, `btnHero`(SOS 전용, `--gradient-hero` 사용) 신규 추가, `chipWarning`/`chipDanger`/`chipSuccess` 추가
- **앞으로 색을 바꿀 땐 이 두 파일만 수정하면 됨** — 개별 컴포넌트에 hex 하드코딩 금지

### 1차 적용 — `/myteam` (프로토타입)
그라데이션 27곳 + 색 있는 글로우 섀도우 5곳을 전부 위 토큰으로 교체(로직·상태·이벤트 핸들러는 무수정, 스타일 값만 변경):
- 매장별로 랜덤 색을 돌리던 스택 카드 그라데이션(`CARD_GRADIENTS` 6색) → 고정 무채색 톤 하나로 통일
- 출근/퇴근/서명/공고활성화 등 액션 버튼 그라데이션 → `var(--success)`/`var(--danger)`/`var(--primary)` 단색
- 대타 SOS 배너만 `var(--gradient-hero)` 유지(유일한 허용 예외)

### 2차 적용 — 확산 완료 (2026-07-22)
`/daeta`, `AppHeader`, `BottomNav`, `DaetaSosHome`, `DaetaWorkerHome`까지 동일 토큰 적용 완료. `/daeta`에서만 그라데이션 32곳 정리(후보 카드마다 랜덤 5색 배경 → 무채색 통일 포함). 각 화면당 그라데이션은 SOS 버튼/생성 CTA 등 `--gradient-hero` 1곳만 남김. 로고 워드마크(PAZAB 텍스트)는 브랜드 예외로 유지.

### 3차 — 이모지 → 아이콘폰트 교체 (2026-07-22)
같은 화면 안에서 이모지(기기마다 다르게 렌더링)와 Tabler 아이콘폰트가 섞여 있던 것이 "앱 같지 않음"의 또 다른 큰 원인으로 지목됨. 위 6개 파일 기준으로 **기능 아이콘 역할**(섹션 헤더, 버튼, 상태 배지, 아이콘 박스, 통계 라벨) 100곳 이상을 Tabler 아이콘(`ti ti-*`)으로 교체.

**의도적으로 유지한 것** (규칙으로 고정):
- 알림/푸시/토스트 메시지 본문의 이모지 — OS 알림함이 자체 폰트로 렌더링해 기기별 불일치 문제가 없음
- 감정적 순간의 이모지 — 🎉(매칭 성사), 😅😴(빈 상태), 🙋‍♂️(캐주얼 제안) 등
- HEXACO 성격유형 아이콘 맵(16종) — 캐릭터성이 의도된 콘텐츠
- `✓`/`✕` 같은 순수 타이포그래피 글리프 — 색 있는 이모지가 아니라 기기 간 렌더링 차이가 없음

### 4차 — 남은 화면 확산 완료 (2026-07-24)
`/chat`, `/chat/[id]`, `/mypage`, `/explore`, `/job/[id]`, `/worker/[id]` 전부 동일 토큰·아이콘 규칙 적용 완료. 병행해서 발견된 라이트모드 파손 버그도 같이 수정:
- 헤더/입력창 배경이 `rgba(var(--bg-rgb, 24,24,27),...)` 식으로 존재하지 않는 CSS 변수를 참조해 다크 리터럴로 항상 고정되던 것 → `var(--nav-bg)`로 교체
- `components/daeta/DaetaRegisterModal.tsx`는 이번 확산 대상에 없었지만, 라이트모드에서 텍스트가 아예 안 보이는 버그(모달 전체에 `color:"#fff"` 하드코딩)가 발견돼 별도로 전체 토큰화. 매장 선택도 `<select>` 드롭다운 → 버튼 칩 방식으로 변경.

남은 화면(`/pre-meet`, `/interview`, `/onboarding` 등)은 발견되면 그때그때 동일 규칙으로 처리.

---

## 10. 타이포·터치 스케일 상향 — 60대 타겟 접근성 기준 정합화 (2026-07-25)

### 진단
"60대도 초보자도 쉽게" 목표를 실제 접근성 기준과 대조한 결과, **색·아이콘·그라데이션 규칙(방향 A)은 유효하지만 크기 스케일이 목표 타겟에 비해 작게 잡혀 있음**을 확인:

| 항목 | 현재 (`lib/styles.ts`, `app/globals.css`) | 권고 기준 | 근거 |
|---|---|---|---|
| 본문 텍스트 | 13~14px | 최소 16px | KWCAG 2.2, Google Android 접근성 가이드 |
| 버튼/CTA 텍스트 | 14~15px | 16~20px (경고문 포함) | 동일 |
| 칩/뱃지·`btnSmall` | 11~12px | 정보 표시용은 예외 가능하나 탭 대상이면 16px | 동일 |
| 토글 스위치(`toggleTrack`) | 44×24px | 48×48dp 이상 | Google 접근성 고객센터, KWCAG 컨트롤 크기·간격 |
| `btnSmall` 실측 높이 | 약 26~30px | 48×48dp 이상 | 동일 |
| `--text-muted` 다크모드 (`#71717a` on `#0a0a0a`) | 대비비 ≈ 4.09:1 | 4.5:1 이상(본문) | KWCAG/WCAG 2.1 AA (실측 계산, 라이트모드는 4.84:1로 통과) |

이 수치들은 방향 A 도입 당시(§9, 2026-07-22) 목표였던 "토스·카카오식 정돈된 밀도"에 맞춰졌던 것으로, 20~40대 범용 핀테크 앱 기준으로는 정상 범위지만 60대 타겟에는 못 미침.

### 결정
**방향 A의 색·그라데이션·아이콘 규칙은 그대로 유지**(§4, §9는 폐기하지 않음). 상향 대상은 크기 스케일뿐:

- 본문/리스트 텍스트: 14px → **16px**
- 버튼·CTA(`btnPrimary`/`btnSecondary`/`btnAccent`/`btnHero`/`btnGhost`/`btnDanger`): 14~15px → **16~17px**
- 섹션 헤더(`sectionHeader`): 15px → **17px**
- 경고·할증·마감임박 등 즉시 인지가 중요한 텍스트: **18~20px** (KWCAG: 18pt 이상이면 명도 대비 3:1까지 허용되는 별도 기준선이기도 함)
- 모든 탭 가능한 컨트롤(토글·`btnSmall`·아이콘 버튼 포함) 실측 높이: 44px → **48px 이상**, 인접 컨트롤 간 최소 **8px** 간격
- `--text-muted` 다크모드 값: 대비 재계산 후 조정 (현재 `#71717a` → 4.5:1 이상을 만족하는 값으로, 예: `#8a8a94` 계열 — 최종 값은 실제 배경 위에서 재검증 후 확정)
- 순수 표시용(탭 불가능한 상태 뱃지·라벨)은 11~12px 유지 가능 — 위 기준은 **읽어야 하거나 눌러야 하는 요소**에만 적용

### 적용 범위
방향 A 확산 때와 동일한 방식으로 점진 적용: 신규 컴포넌트부터 새 스케일 사용, 기존 화면은 발견되는 대로 마이그레이션. `lib/styles.ts`·`app/globals.css` 두 파일만 고치면 전체 반영되는 구조는 §4의 "색은 두 파일만 고치면 된다" 원칙과 동일하게 유지.

### 참고
- [Google 접근성 고객센터 — 터치 영역 크기](https://support.google.com/accessibility/android/answer/7101858?hl=ko)
- [한국형 웹 콘텐츠 접근성 지침(KWCAG) 2.2](https://a11ykr.github.io/kwcag22/)
- [고령층 UX 디자인 가이드 — 글자 크기 16px 이상](https://scingnews.com/34)
- [토스, 시니어 사용자가 어려워하는 UX 5가지](https://toss.tech/article/senior-usability-research)

---

## 11. 정산·결제 인프라 설계 (2026-08-12)

### 진단
대타 등록 화면의 "노쇼 안심 보장 수수료"·"계좌 1원 실명인증"과, 명세서 자동발행의 "자동 지급"이 실제로는 전부 화면만 있고 뒷단이 없는 상태로 확인됨:

- `components/daeta/DaetaRegisterModal.tsx` — `secureOption` 체크 시 `daeta_postings.secure_option`에 boolean만 저장, 실제 3,000원 결제도 노쇼 시 위로금 환불도 없음. 1원인증(`verifyAccount()`)은 입력값과 무관하게 항상 "인증완료"(예금주: 안심사장) 처리되는 목업.
- `app/api/cron/payslip/route.ts` — 근태 기반 명세서 **계산·발행**은 자동화돼 있으나 실제 계좌이체는 없음("자동 지급"이 아니라 "자동 계산"). `payslips` 테이블(`supabase/v2_schema.sql:263`)에 지급여부를 추적하는 컬럼 자체가 없음(`paid_status`/`paid_at` 등 부재).
- 코드베이스 전체에 PG(토스페이먼츠/포트원 등)·오픈뱅킹 연동이 전무함.
- 계좌정보 자체는 이미 암호화 인프라가 있음 — `lib/bankCryptoServer.ts`/`lib/bankCryptoClient.ts`(AES-256-GCM), `app/contract/page.tsx`의 `bankName`/`bankNumber` 필드로 계약서에 저장됨. 결제 인프라를 얹을 때 이 계좌정보를 그대로 재사용 가능.

### 단계별 설계

**1단계 — 딥링크 원탭 송금 (PAZAB이 자금을 전혀 보관·경유하지 않음, 즉시 착수 가능)**
- `payslips`에 `paid_status`('unpaid'|'paid' 기본 unpaid), `paid_at`, `paid_method`('deeplink'|'manual') 컬럼 추가.
- 명세서 상세/팀원 상세 페이지에 "💸 지급하기" 버튼 → 바텀시트에서 계좌정보 복호화(`decryptBankFields`) 후 노출, 은행 앱 송금 딥링크(토스/카카오뱅크 등, 정확한 URL 스킴은 각 사 공식 문서로 착수 직전 재검증 필요) + **딥링크 실패 대비 필수 폴백**(계좌번호·금액 클립보드 복사 버튼)까지 함께 제공.
- 실제 이체 성공 여부는 PAZAB이 알 수 없으므로 "지급완료로 표시"는 사장님 자기신고 방식(수동 체크) — 법적 리스크 없음, 사업자 계약 불필요.
- 한계: 최종 이체 승인은 은행 앱에서 사장님이 직접 해야 함(완전자동 아님), 노쇼 위로금 자동환불은 이 방식으로 불가능(PAZAB이 돈을 안 갖고 있음).

**2단계 — PG 지급대행 연동 (설계만 확정, 실제 착수는 사업자 계약 이후)**
- 안심보장 수수료(3천원) 징수: 표준 PG 결제창(포트원/토스페이먼츠 등) 단건결제. 신규 테이블/필드 예상: `daeta_postings.payment_id`/`paid_amount`/`paid_at`, `/api/payments/charge`(결제요청), `/api/payments/webhook`(PG 콜백 검증) 신설.
- 위로금 자동환불·임금 자동이체: PG사의 지급대행(대량이체) API 필요 — PG사와 정산계약·예치금 운용계좌가 전제조건.
- **임금 자동이체는 근로기준법 제43조 임금 직접지급원칙과 충돌 소지** — "사용자(사장님) 동의 하에 PAZAB이 이체를 대행한다"는 이용약관 조항과 법률 검토가 선행돼야 함(저는 법률 자문 아님, 실제 진행 전 변호사 확인 필수).
- 필요한 사업자측 준비물: PG 가맹계약, 정산 전용 은행계좌, 통신판매업 신고 여부 확인.

**3단계 (장기) — 노쇼 보장 재원 구조**
- 수수료를 모아 위로금을 지급하는 구조는 실질적으로 소액 보증상품 판매에 해당할 소지가 있어 보험업법 해당 여부 검토가 필요함. 대안으로 외부 소액단기보험사 제휴도 검토 가능하나 이 단계는 지금 우선순위 아님.

### 결정
지금 시점(콜드스타트, 마켓플레이스 밀도 확보가 최우선 — STRATEGY.md)엔 **1단계만 실제 착수 대상**. 2·3단계는 이 설계를 기준으로 사업자 계약·법률 검토가 끝난 뒤 착수. 위 설계와 별개로, "노쇼 시 위로금 전액 환불" 문구·체크박스와 가짜 1원인증은 실제 지급 능력이 없는 한 소비자 기만 소지가 있어 **"준비 중" 처리(비활성화 또는 라벨 교체)가 1단계 착수 여부와 무관하게 우선 필요** — 별도 세션에서 처리.

### 후속 결정 — "노쇼 안심 보장 수수료" 자체를 폐기 (2026-08-13)
결제 기반 노쇼 보장(3단계 재원 구조)은 보험업법·PG계약 부담 대비 지금 단계 가치가 낮다고 판단, **유료 위로금 모델 자체를 포기**하고 무료 대안 3가지로 대체(구현 완료):

- **A. 신뢰 시그널 노출** — `DaetaRegisterModal.tsx`의 "🛡️ 노쇼 안심 보장 수수료" 체크박스(결제 유도)를 제거하고, "✅ 노쇼 이력 없는 검증 인력에게 먼저 노출돼요" 등 이미 존재하는 trust_score/Tier 시스템을 그대로 설명하는 무료 안내 박스로 교체.
- **B. 억제책 노출** — 같은 박스에 "노쇼한 알바생은 신뢰점수가 크게 깎이고 일정 기간 대타 참여가 제한돼요" 문구 추가. 실제로 이미 동작하는 로직(`app/api/daeta/complete/route.ts`의 -30점, `app/api/daeta/cancel/route.ts`의 3~14일 정지)을 사장님에게 처음으로 명시.
- **C. 속도 기반 자동 재확산 (핵심)** — 기존엔 노쇼 신고 시 `daeta_postings.status`가 `cancelled`로 끝나고 사장님이 알아서 다시 구해야 했음(`app/api/daeta/complete/route.ts` noshow 분기). 이제 노쇼 신고 즉시: 공고를 `pending`으로 재오픈 → `escalation_stage`를 (팀 재알림은 유지하되) 이미 동의한 상한(`allow_new`) 안에서 넓은 단계로 즉시 점프 → `lib/daetaEscalation.ts`의 `notifyTeam`/`notifyNearby`를 그 자리에서 바로 호출(5분 크론 대기 없이) → 사장님에게 "🔄 노쇼 확인, 바로 대체 인력을 찾고 있어요" 알림. 노쇼한 당사자는 `notifyTeam`/`notifyNearby`에 신설한 `excludeWorkerId` 파라미터로 재알림 대상에서 제외.
- `secure_option` 컬럼은 DB에 남겨두되(과거 데이터 보존) 신규 저장 경로는 없앰 — `app/d/[code]/DaetaPreviewClient.tsx`의 관련 배지 렌더링도 같이 제거(원래 "안심보장"이 아니라 "보건증 자격요건 필요"라는 다른 문구를 오표시하던 버그였음).

### 후속 결정 — 노쇼 자동판정 타임라인 신설 (2026-08-13)
"C. 속도 기반 자동 재확산"은 지금까지 사장님이 수동으로 "노쇼 신고"를 눌러야만 발동했음 — 정작 "왔는지 안 왔는지"를 판가름할 타이밍 기준 자체가 없었음. 정규 팀원 출퇴근에는 이미 검증된 타임라인 로직(`app/api/cron/checkin/route.ts`: 10분전→정시→+5분지각경고→**+20분 자동결근**)이 있는데 대타(`daeta_postings`/`matches`)엔 체크인 개념 자체가 없어서 이 크론이 안 봤음. 같은 패턴을 대타에 이식하되 대타는 그 시간에 반드시 사람이 있어야 하는 긴급성이 커서 **+15분**으로 더 타이트하게 설정:

| 시점 | 동작 |
|---|---|
| 시작 10분 전 / 정시 | 알바생에게 출근 알림 + 원탭 체크인(`daeta_checkin` 푸시 액션) |
| +5분 | 알바생·사장님 둘 다 지연 경고. 사장님 알림엔 `daeta_extend`(10분 연장) 원탭 버튼 동봉 |
| +15분 (연장 안 걸려있으면) | 자동 노쇼 확정 → `lib/daetaNoShow.ts`의 `markNoShowAndReescalate()` 호출(=수동 신고와 동일 처리: -30점 + 즉시 재확산) |

구현: `matches`에 `checked_in_at`/`noshow_extend_until` 컬럼 신설(`supabase/patch_daeta_noshow_auto.sql`) · `app/api/daeta/checkin`(알바생 체크인) · `app/api/daeta/extend`(사장님 10분 연장, 앱 버튼+푸시 원탭 둘 다) · `app/api/cron/daeta-checkin`(5분 주기 판정) · `public/sw.js`에 `daeta_checkin`/`daeta_extend` 푸시 액션 핸들러 추가 · `DaetaHistoryView.tsx`에 알바생용 "✅ 출근했어요" 버튼과 사장님용 "⏳ 10분만 더"/출근상태 표시 추가. 노쇼 신고 로직은 수동(`app/api/daeta/complete`)·자동(크론) 양쪽이 `markNoShowAndReescalate()` 하나를 공유 — 중복 구현 방지.

### 후속 결정 — 전체 플로우 재점검, 이탈 처리 구멍 2건 수정 (2026-08-13)
노쇼 자동판정을 만든 김에 전체 대타 플로우(등록→확산→지원/수락→출근→정산→이탈)를 다시 훑어서 같은 유형의 구멍을 추가로 찾음:

- **사전 취소가 노쇼보다 대우가 나빴던 버그**: `app/api/daeta/cancel/route.ts`가 취소 페널티(90일 누적 가중 정지+감점)만 적용하고 `daeta_postings`는 아예 안 건드려서, 노쇼보다 훨씬 "착한" 사전 취소가 오히려 재구인 시도조차 없이 방치되고 있었음. `lib/daetaEscalation.ts`에 신설한 `reescalateAfterDropout()`(노쇼 재확산 로직에서 신뢰점수 처리 부분만 떼어낸 공용 함수)을 알바생측 취소에도 연결 — 취소해도 노쇼와 동일하게 즉시 재오픈+재확산됨(감점은 취소 전용 페널티만 적용, 중복 감점 없음). 사장님측 취소는 재구인 의사가 없다는 뜻이라 공고를 `cancelled`로 완전 종료.
- **경합 지원자 방치 버그**: 여러 알바생이 같은 대타 공고에 지원했을 때 사장님이 한 명을 수락해도 나머지 지원자의 매칭이 `pending`으로 계속 남아있고 알림도 안 갔음(`app/api/lovecall/route.ts` accept 분기). 수락 시 같은 공고의 다른 pending 매칭을 전부 `rejected` 처리 + "다른 분으로 확정됐어요" 알림 발송하도록 수정.
- ~~(참고, 미착수) 동시 수락 경합 가드 부재~~ — 2026-08-19 아래 항목에서 해결.
- **결제/자동지급 우선순위 재확인**: 지금은 전부 무료 제공 단계라 결제 인프라 투자는 계속 보류. 정산은 이미 계산까지는 자동화됐고 계좌이체 자체는 사장님에게 큰 마찰이 아니라고 판단 — 남은 리소스는 매칭 성사율/속도(위 버그들처럼 실제로 매칭이 새는 지점)에 우선 투입. DESIGN_PLAN.md §11의 1단계(딥링크 송금)도 지금 당장은 착수하지 않음.

### 후속 결정 — 전체 플로우 재점검 2차, 임금 정확성·동시성·재사용 구멍 다수 수정 (2026-08-19)
1차 재점검(위 2026-08-13) 이후 정산/동시성/스키마 관점으로 다시 훑어서 발견한 것들. 전부 구현 완료:

- **출근 후 취소·노쇼 시 급여 증발 버그**: `app/api/daeta/cancel/route.ts`·`app/api/daeta/complete/route.ts` 둘 다 `checked_in_at` 여부를 확인 안 하고 처리해서, 실제로 몇 시간 일한 사람이 취소/노쇼 처리되면 그 시간에 대한 급여가 통째로 사라졌음. 공용 정산 계산을 `lib/daetaSettlement.ts`로 뽑아서: 출근 후 취소 시 실근무시간만큼 자동 payslip 발행, 이미 출근한 사람은 노쇼 신고 자체를 막고 "완료·정산"으로 유도.
- **다일치 공고 2일차 이후 노쇼 감지 전무**: `matches.checked_in_at`이 1일차 값 하나뿐이라 2일차 이후 무단 불참을 시스템이 아예 몰랐음. `daeta_daily_attendance` 테이블 신설(`patch_daeta_daily_attendance.sql`, 다일치 전용 — 하루짜리는 기존 그대로 `matches` 컬럼이 SOT)로 일자별 출퇴근 기록, 크론이 2일차 이후도 미출근 알림, 노쇼 확정 전 이미 완료한 날짜는 `settlePriorDailyAttendance()`로 먼저 정산.
- **대타 정산에 세금 계산 자체가 없었음**: 정규 팀원 일급 정산(`cron/payslip`)엔 있는 `calcDailyWorkerTax` 원천징수 계산이 대타(`daeta/complete`)엔 빠져서 항상 세전 금액이 "실수령액"으로 안내되고 있었음. `payslips.income_tax/local_tax/net_pay` 채우도록 수정, 이력 화면에 세전/세후 구분 표시.
- **동시 수락·중복 정산 가드**: 위에서 "미착수·후순위"로 남겨뒀던 동시 수락 경합 가드를 `lib/daetaEscalation.ts`의 단계전진 가드(`.eq("escalation_stage", stage)`)와 동일 패턴으로 구현(`daeta_postings.status`를 pending일 때만 원자적으로 matched 전환). 정산완료(`daeta/complete`)도 동일하게 `matches.progress_status`를 accepted일 때만 hired로 전환하는 가드 추가 — 둘 다 더블클릭/다른 탭 동시 요청으로 계약·명세서가 중복 생성되던 구멍.
- **계좌정보 재사용 불가 — 스키마 정규화**: §11 진단(2026-08-12)에서 "계좌정보는 이미 암호화 인프라가 있다"고 적어뒀지만, 실제로는 `contracts.contract_data`에만 계약 건별로 저장돼서 재계약·대타 여러 건마다 매번 새로 입력/암호화해야 했음. `users.bank_name/bank_number_enc/bank_account_enc`(`patch_users_bank_account.sql`)를 SOT로 신설 — `app/contract/page.tsx` 저장 시 write-through, `app/api/lovecall/route.ts`의 대타 자동계약 생성 시 이 값을 그대로 복사해서 채움(암호문 그대로 이관, 재복호화 없음). `contracts.contract_data.bankAccount/bankNumber`는 계약 체결 시점 스냅샷으로 계속 유지(역할 분리). **잔여 갭**: 대타 계약서 PDF(`app/api/contract/route.ts`)는 `pay_method` 라벨만 출력하고 실제 계좌번호를 사장님에게 보여주는 화면이 없음 — §11 1단계(딥링크 송금)와 별개로 후속 필요.
- **최저시급 검증 하드코딩**: `DaetaRegisterModal.tsx`가 `fetchMinWage()`로 최신 최저시급을 가져와 안내문구엔 쓰면서 실제 등록 차단 검증은 리터럴 `10030`을 쓰고 있었음(연도 바뀌면 안내와 실제 기준이 어긋남) — `minWage` state로 통일.
- **대타 출근 체크인에 위치 검증 부재**: 정규 팀원 출퇴근(`myteam.tsx` `CheckInButton`)엔 있는 매장 반경(200m) 체크가 대타 체크인엔 없어서 아무 데서나 "출근했어요"가 통과됐고, 이 시각이 그대로 노쇼 면제/실근무시간(→급여) 판정에 쓰였음 — 동일 반경 검증 추가.
- **체크인 타이밍 갭**: 체크인 자체엔 시간 제한이 없어서 근무 시작 몇 시간 전에도 눌러버릴 수 있었음 — 크론의 "10분 전" 알림 시점과 맞춰 체크인 버튼도 그때부터만 활성화(서버·클라이언트 양쪽).
- **정산 타이밍 갭 2건**: "근무 완료·정산하기"가 (a) 근무 시작 전, (b) 예정 종료 전에도 항상 눌려있었음 — (a)는 하드 차단(서버·클라이언트), (b)는 조기 종료의 정당한 케이스가 있어 하드 차단 대신 강한 경고로 처리.
- **지원 알림이 막다른 길이었던 버그**: 대타 지원 알림을 누르면 `/worker/[id]` 프로필로 갔는데, 그 페이지는 대타 매칭을 아예 제외하고 정규 채용만 다뤄서 수락할 방법이 없었음 — 지원자 목록(`/daeta?applicants=`)으로 직접 딥링크하도록 변경.
- **자동계약서 가짜 사업자등록번호**: `app/api/lovecall/route.ts`의 대타 자동계약이 `bizRegNo: "123-45-67890"` 더미값을 하드코딩하고 있었음 — `employer_profiles.biz_reg_number`에서 실제 값 조회하도록 수정.
- **등록 최소 리드타임 부재 + 만료시각 타임존 버그**: 대타 등록에 하한이 전혀 없어 근무 10분 전에도 등록 가능했음(에스컬레이션 사다리가 돌 시간이 없어짐) — `daeta_sos_config.min_lead_minutes`(기본 60분) 신설. `expires_at`을 `Z`(UTC 고정)로 저장해 실제보다 9시간 늦게 만료되던 버그도 `+09:00` 명시로 수정.
- **죽은 코드**: `components/daeta/DaetaWorkerHome.tsx`(592줄) — import만 되고 어디서도 렌더링 안 되는 상태로 방치돼있어 삭제.
- 그 외 UX: 지원자 수락 시트에 Tier 배지 노출, 노쇼 신고 이의제기 경로(`report-unpaid`와 대칭) 신설, 취소/노쇼 시 채팅 시스템 메시지 추가, 이력 화면 상태 배지에 "근무 중" 실시간 강조 표시.

구현 상세는 `db-schema.md`(users/daeta_daily_attendance 컬럼), `supabase/patch_users_bank_account.sql`·`patch_daeta_daily_attendance.sql`·`patch_daeta_min_lead.sql` 참조.

### 후속 결정 — `/daeta` 홈 정리: 내 SOS 현황 화면 분리, 카드 겹침·스크롤 버그 수정 (2026-09-04)
`/daeta` 홈에 "내 공고" 관리(수정/취소/지원자 확인)를 리스트 최상단에 `position: sticky`로 얹어뒀던 게, 공고가 여러 개로 늘어날수록 스크롤 시 아래 "긴급"/"다른 공고"를 계속 덮어 겹쳐 보이는 근본 원인이었음(sticky는 짧은 헤더용이지 늘어나는 리스트 전체를 감싸면 안 됨). 다른 서비스(당근알바/알바몬/에어비앤비 호스팅 등)도 "내가 올린 것 관리"와 "남의 것 둘러보기"를 화면 자체로 분리하는 걸 참고해 구조를 바꿈:

- **`/daeta/my` 신설**: 내 공고 관리 전용 화면. `DaetaSosHome`에 `mode="myOnly"` 프롭을 추가해 기존 데이터 로딩(`matchMeta`, `postings` 등)과 모달(수정/취소/지원자 시트/상세 팝업)을 그대로 재사용 — 컴포넌트 중복 없이 화면만 갈라짐. `/daeta`엔 "🔥 내 SOS 진행 중 (N건) →" 작은 배너 하나만 남기고 이동은 이 배너로.
- **상단 3개 상태 줄(대타 가능 토글 / 내 SOS 진행 중 / 대타 이력)을 가로 3분할 컴팩트 타일로 통일**: 세로로 큰 카드 3장이던 걸 원형 아이콘(28px)+짧은 라벨 한 줄짜리 타일로. 라벨은 맥락 없이도 알아보게 "대타 ON/OFF", "내 SOS N건", "대타 이력 N건"으로 통일.
- **`PostingCard`(`components/daeta/DaetaSosHome.tsx`) 텍스트·버튼 겹침 버그**: 텍스트 블록(좌)과 액션 버튼(우)을 한 행에 나란히 두던 구조가, 좁은 카드(긴급 섹션, `width=280`)에서 상호명이 길면 버튼과 부딪히거나 겹쳐 보였음. 버튼을 텍스트 블록 **아래 별도 줄**로 완전히 분리해 카드 폭과 무관하게 안 부딪히게 함. `/daeta/my`의 수정/취소 버튼은 관리 화면의 핵심 액션이라 행 전체를 채우는 큰 버튼(`flex:1`)으로 키움.
- **가로 스크롤(긴급 섹션) 개선**: `scrollSnapType`을 `mandatory`→`proximity`로 완화(강제 스냅 때문에 살짝만 밀어도 원위치로 튕겨 "스크롤이 안 된다"처럼 느껴지던 문제), PC에서 Shift 없이 일반 마우스 휠로도 가로 스크롤되도록 `onWheel` 핸들러 추가(세로 위주 입력만 가로로 변환, 트랙패드 진짜 가로 제스처는 그대로 둠).
- **데스크톱 스크롤바 숨김**: 모바일은 원래 스크롤바가 안 보이는데 데스크톱 크롬 등은 항상 그려서 앱처럼 안 보였음 — `.no-scrollbar` 유틸리티(`app/globals.css`)를 목록형 스크롤 영역과 `html` 전역에 적용. 스크롤 기능은 그대로, 시각적으로만 숨김.
- **`대타 이력`(`DaetaHistoryView.tsx`) "펼치기" 버튼 → 무한 스크롤**: 처음 1건만 접어 보여주고 버튼을 눌러야 전체(많으면 그 영역만 중첩 스크롤박스)가 나오던 방식을, 5건씩 보여주고 페이지 하단 근처로 스크롤하면 자동으로 5건씩 더 풀리는 방식으로 전환. 데이터는 이미 최대 200건 한 번에 불러와져 있어 추가 네트워크 요청 없음.

**잔여 갭(미착수)**: "긴급" 판정 기준(현재 오늘 근무 OR 3시간 이내 시작 OR 확산 3단계)이 서로 다른 성격(시간적 급함 vs 매칭 안 되는 신호)을 한 리스트 배치 기준에 섞어놔서 혼란 소지 있음 — "오늘 근무 여부" 하나로 단순화하고 확산 3단계는 카드 배지로 분리하는 안을 논의했으나 미결정. 대타 공고 노출에 지역/반경 제한이 전혀 없어(쿼리에 geo 필터 없음) 먼 거리 공고도 그대로 노출되는 것도 STRATEGY.md의 "탕정/아산 한 상권 집중" 원칙과 어긋나는 구현 갭으로 확인됨 — 별도 논의 필요.

---

## 배포 전 수동 작업 (필수)

1. **SQL 패치 실행**: `supabase/patch_daeta_sos.sql` → Supabase SQL Editor에서 실행 (escalation 컬럼 + daeta_sos_config 테이블). 미실행 시 대타 등록이 실패함. 추가로 `patch_daeta_auto_premium.sql`, `patch_daeta_employer_profile_link.sql`도 실행 필요(2026-08-07 추가분). **`patch_daeta_noshow_auto.sql`도 신규 실행 필요(2026-08-13 추가분, `matches.checked_in_at`/`noshow_extend_until`) — 미실행 시 자동 노쇼 판정 크론이 에러남.** **2026-08-19 추가분 3건도 순서대로 실행 필요**: `patch_users_bank_account.sql`(users 계좌정보 SOT), `patch_daeta_daily_attendance.sql`(다일치 노쇼 감지용 — 미실행이어도 하루짜리 체크인/체크아웃은 에러를 삼키고 정상 동작함), `patch_daeta_min_lead.sql`(등록 최소 리드타임, `daeta_sos_config.min_lead_minutes` — 미실행 시 기본값 60분으로 코드에서 폴백되므로 필수는 아님).
2. **크론 등록**: cron-job.org에 `GET /api/cron/daeta-escalate` 5분 주기 — ✅ 2026-08-07 등록 완료(그 전엔 코드만 있고 실제 등록이 안 돼 있었음, 여러 핸드오버 문서에 "확인 필요"로만 남아있던 항목). `GET /api/cron/doc-expiry`(팀원 보건증 만료 알림, 매일 1회)도 신규 등록 완료. **`GET /api/cron/daeta-checkin`(대타 출근 타임라인, 5분 주기) 신규 등록 필요(2026-08-13 추가분) — 미등록 시 자동 노쇼 판정이 전혀 동작하지 않음.** 전부 `Authorization: Bearer <CRON_SECRET>` 헤더 필요.
