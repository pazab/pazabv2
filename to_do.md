# TO-DO: 사장님 플로우 UX 개선 및 계약서 시스템

> 최종 업데이트: 2026-08-20 | 우선순위 순 정렬

---

## ⑨ 대타 SOS 업무 사진 등록 + 죽어있던 뱃지 활성화 ★★☆ [완료]

**문제:** 일본 초단기 알바 앱(타이미 등)은 업무 사진으로 인터뷰 없이도 미스매치·노쇼를 줄이는데, 대타 공고엔 텍스트뿐이었음. 또한 `BADGE_DEFS`에 "고평점⭐"·"즉시출근⚡" 뱃지가 정의만 돼 있고 실제로 수여하는 로직이 어디에도 없어 아무도 받을 수 없는 죽은 뱃지였음.

**완료 (2026-08-20):**
- 대타 등록/수정 폼에 업무 사진 업로드(최대 3장), 기존 `ImageCropModal`로 구도 보정(1:1), 이전에 올린 사진 재사용 그리드, HEIC 변환 프리로드(선택 시 "멈춘 것처럼" 보이던 지연 해소)
- 사진을 지원자 목록 카드·상세모달·카톡 딥링크 미리보기(`/d/[code]`)에 노출
- `checkAndAwardDaetaBadges()` 신설 — 대타 완료 시 사장님 평가 3건+평균 4.5점 이상이면 고평점, 당일 등록된 긴급요청을 3회 이상 수락 완료했으면 즉시출근 뱃지를 자동 수여
- 지원자 목록(사장님이 수락 여부를 정하는 결정 순간)에 뱃지 칩 노출 — 새 테이블 없이 기존 `user_badges`/`getBadgesByRole` 재사용이라 `/worker/[id]`·`/mypage`·`/store/[id]`에도 자동 반영됨

**보류:** 당일 즉시 정산(即時払い) — 임팩트는 크지만 작업량이 큰 편이라 다음 순번으로 미룸

**관련 파일:** `components/daeta/DaetaRegisterModal.tsx`, `components/daeta/DaetaSosHome.tsx`, `app/d/[code]/DaetaPreviewClient.tsx`, `lib/trustScore.ts`, `app/api/daeta/complete/route.ts`, `supabase/patch_daeta_images.sql`

---

## ⑦ 근로계약서 인라인 입력·동의 및 미성년 감지 ★★★ [완료]

**개선 사항 (2026-07-24 완료):**
- **계좌 입력 먹통 완벽 해결**: `bankName`, `bankNumber`, `bankCustomName` 독립 상태 분리로 계좌번호 입력 시 드롭다운 꼬임 버그 해결
- **카카오 5자리 우편번호(`zonecode`) 연동**: 5자리 우편번호 추출 및 `contracts.contract_data` + `users` 테이블 역Sync
- **2단 상세주소 입력창 및 독립 행 배치(`colSpan: 3`)**: 도로명 주소 + 상세주소 2단 분리 및 독립 행 배치로 PC/모바일 레이아웃 밀림 방지
- **읽기 전용 요약 확인 모달**: 계약서 본문 직관적 기입 후 동의 클릭 시 이중 입력 없이 요약 카드 모달 노출 후 원클릭 최종 서명
- **미성년자(만 18세 미만) 나이 감지 경고 모달**: 생년월일 선택 시 만 나이 계산, 만 18세 미만 시 법적 필수 안내 및 오타 수정 팝업 지원
- **PC 반응형 커스텀 카드 레이아웃 (`maxWidth: 540`)**: 대형 모니터 횡 찢어짐 방지

**관련 파일:** `app/contract/page.tsx`, `app/contract/view/page.tsx`, `app/chat/[id]/page.tsx`

---

## ① 계약서 템플릿화 ★★★ [부분 완료]

**문제:** 매번 20개+ 항목 처음부터 입력, 같은 매장 두 번째 직원도 동일 반복  
**완료:**
- `/contract` 진입 시 "이전 계약서 불러오기" 버튼 제공 (2026-07-11 버그 수정)
  - 현재 선택된 매장(`employer_profile_id`) 기준으로 해당 매장 최신 계약서 조회 (기존: 사장님 전체 계약서 혼용)
  - 불러오기 시 임금·근무시간·요일·보험 조건만 복사, 근로자 정보·날짜·사업체 정보는 현재 값 유지

**잔여 개선 사항:**
- 매장 등록 시 기본 근무조건(시급·요일·시간) 저장 → 계약서 자동채움 소스로 활용

**관련 파일:** `app/contract/page.tsx`, `app/employer/register/page.tsx`

---

## ② 사장님 첫 진입 온보딩 가이드 ★★★ [완료]

**문제:** 가입 후 `/myteam` 진입 시 빈 화면, 다음 행동 불명확  
**완료 (2026-08-07):**
- 매장 미등록 상태일 때 빈 화면 대신 "3단계 시작 가이드" 표시
  1. 매장 등록하기
  2. 직원 초대하기 (STRATEGY.md 초대 공급 경로 원칙에 따라 "공고 올리기" 옵션은 제거, 초대 단일 액션으로 정리)
  3. 근로계약서 작성
- 각 단계 완료 시 체크 표시 및 다음 단계 CTA 구현됨

**관련 파일:** `app/myteam/page.tsx`

---

## ⑧ 알바생 첫 진입 가이드 ★★☆ [완료]

**문제:** 사장님 쪽엔 "3단계 시작 가이드"가 있는데 `/explore`로 착지하는 알바생 쪽엔 아무 안내가 없었음(비대칭). STRATEGY.md §11.2 우선순위상 콜드 신규 알바생은 최하위지만, 초대돼 들어온 직원·대타 콜드유입 알바생도 첫 화면 안내가 전무한 건 방치였음.  
**완료 (2026-08-08):**
- `/explore` worker 탭 첫 진입 시 1회성 안내 배너 추가(하트=지원, 성향분석=궁합점수, 긴급순=대타). 닫으면 `localStorage`에 기록돼 다시 안 뜸.
- 사장님 쪽 3단계 체크리스트처럼 무겁게 만들지 않음 — 이미 `worker_profiles`가 첫 러브콜 시 자동 생성되는 등 강제 단계가 없는 구조라, 체크리스트보다 가벼운 안내 배너가 기존 progressive-disclosure 원칙에 더 맞다고 판단.

**관련 파일:** `app/explore/page.tsx`

---

## ③ 급여 자동발행 설정 ★★☆

**문제:** 매월 사장님이 직접 팀원 상세 → 급여탭 → 발행 수동 진행  
**개선:**
- 팀원별 "매월 N일 자동발행" 토글 설정
- 설정 시 크론(`/api/cron/payslip`)이 해당 일자에 자동 계산·발행·알림 전송
- 알바생에게 급여명세서 알림 자동 발송

**관련 파일:** `app/employer/team/[id]/page.tsx`, `app/payslip/page.tsx`, `app/api/cron/`

---

## ④ 채용 확정 후 넥스트 스텝 배너 ★★☆ [완료]

**문제:** "채용됐어요 🎉" 이후 계약서 작성 버튼이 시스템 메시지 안에만 있어 사장님이 놓침  
**개선:** (완료됨)
- 채용 확정(hired) + 계약서 미작성 상태일 때 채팅방 상단에 고정 배너 표시
  - "📄 근로계약서를 작성해주세요 → [작성하기]" (사장님용) 및 대기 안내 배너(알바생용) 구현 완료
- 계약서 작성 완료 시 배너 자동 사라짐

**관련 파일:** `app/chat/[id]/page.tsx`

---

## ⑤ 매장 등록 2단계 분리 ★☆☆

**문제:** 매장 기본정보 + 공고 + 근무조건이 한 페이지에 혼재, 이탈률 높음  
**개선:**
- Step 1: 매장 기본정보 (이름·업종·주소·사진) — 1회 등록 후 재사용
- Step 2: 공고 내용 (직종·시급·근무요일·시간·모집인원) — 공고별로 등록
- 매장 정보는 한 번 등록하면 다음 공고 시 자동채움

**관련 파일:** `app/employer/register/page.tsx`

---

## ⑥ 근태 관리 UX 정리 ★☆☆

**문제:** 사장님의 출퇴근 수정 UI가 깊이 숨어 있음, 지각·결근 알림 흐름 불명확  
**개선:**
- 팀원 근태 탭에서 날짜별 출퇴근 시간 인라인 수정 가능하게
- 지각·결근 발생 시 사장님에게 push 알림 (현재 크론 있으나 알림 연결 확인 필요)
- 매장 GPS 좌표 미등록 시 근태 탭 진입 전 경고 안내

**관련 파일:** `app/employer/team/[id]/page.tsx`, `app/api/cron/checkin/route.ts`

---

## 메모

- 계약서 OCR 파싱: 실물 계약서 촬영 → 자동 입력. 기술적으로 가능하나 이미 디지털 폼이므로 실용도 낮음. **템플릿 복사(①)가 더 실용적**
- 우선순위 ①②는 콜드스타트(신규 사장님 이탈 방지)에 직결 — 먼저 작업 권장
